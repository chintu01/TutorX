<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sreyans.tutorx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Teacher.Login.TeacherLoginActivity"></activity>
        <activity android:name=".Student.Login.StudentLoginActivity" />
        <activity
            android:name=".Teacher.Login.TeacherLoginActivity"
            android:label="@string/title_activity_teacher_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Teacher.TeachersHomeActivity"
            android:label="@string/title_activity_teachers_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Student.StudentsHomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Intro.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>